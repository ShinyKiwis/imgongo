.w-72
  .flex.flex-col.h-full
    = action_button 'New Album', new_album_path, class: 'primary-button', turbo_stream: true, turbo_frame: dom_id(Album.new)
    .mt-auto
      = render 'components/uploader'
